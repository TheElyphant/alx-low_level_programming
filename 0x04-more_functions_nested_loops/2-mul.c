#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers.
 * @a: an int to use for the argument
 * @b: an int to use for the argument
 * Return: 0
 */
int mul(int a, int b)
{
	return (a * b);
}
